package core;

public enum FileMetrics {

    LOC,
    LOCTouched,

    numberOfRevisions,
    numberOfAuthors,

    LOCAdded,
    maxLOCAdded,
    averageLOCAdded,

    churn,
    maxChurn,
    averageChurn,

    changeSetSize,
    maxChangeSetSize,
    averageChangeSetSize,

    ageInWeeks,
    weightedAgeInWeeks,

    //isBuggy,
}