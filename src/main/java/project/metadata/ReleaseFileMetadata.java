package project.metadata;

public enum ReleaseFileMetadata {

    releaseVersionOrderID,

    name,

    LOC,
    LOCTouched,

    numberOfRevisions,
    numberOfAuthors,

    LOCAdded,
    maxLOCAdded,
    averageLOCAdded,

    churn,
    maxChurn,
    averageChurn,

    changeSetSize,
    maxChangeSetSize,
    averageChangeSetSize,

    ageInWeeks,
    weightedAgeInWeeks,

    //isBuggy,
}